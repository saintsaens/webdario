import React from "react";
import Typography from "@mui/material/Typography";

const WebMuteCommand = () => {
  return (
      <Typography variant="body2">K: mute</Typography>
  );
};

export default WebMuteCommand;
