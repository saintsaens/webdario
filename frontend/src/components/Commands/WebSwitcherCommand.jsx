import React from "react";
import Typography from "@mui/material/Typography";

const WebSwitcherCommand = () => {
  return (
      <Typography variant="body2">⌘+K: switcher</Typography>
  );
};

export default WebSwitcherCommand;
